{
  "name": "forwardsflow-amplify",
  "version": "1.0.0",
  "description": "ForwardsFlow Platform - AWS Amplify Deployment",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "amplify:push": "amplify push",
    "amplify:publish": "amplify publish",
    "deploy:dev": "amplify env checkout dev && amplify publish",
    "deploy:staging": "amplify env checkout staging && amplify publish",
    "deploy:prod": "amplify env checkout prod && amplify publish"
  },
  "dependencies": {
    "@aws-amplify/ui-react": "^6.0.0",
    "aws-amplify": "^6.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "recharts": "^2.10.0",
    "date-fns": "^2.30.0",
    "axios": "^1.6.0",
    "react-query": "^3.39.0"
  },
  "devDependencies": {
    "@aws-amplify/cli": "^12.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "tailwindcss": "^3.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
